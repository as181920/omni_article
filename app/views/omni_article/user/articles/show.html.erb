<%= content_for :title, @article.title %>

<%= render "omni_article/user/articles/wechat_web_config", article: @article if browser.wechat? %>

<section>
  <div class="card">
    <div class="card-body">
      <%= rich_text_content @article.content %>
    </div>
  </div>
</section>
